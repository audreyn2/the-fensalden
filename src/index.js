import React from 'react';
import ReactDOM from 'react-dom/client';
import TheFensalden from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TheFensalden />
  </React.StrictMode>
);